syntax = "proto3";

package contracts;

option go_package = "contracts";

message Msg {
    string from = 1;
    repeated string recipients = 2;
    map<string, string> headers = 3;
    string subject = 4;
    bytes body = 5;
}
